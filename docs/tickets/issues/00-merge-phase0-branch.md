# 00 — Merge Phase-0 branch to main (or pin agents to it)

**What to build:** Make the current plan + Phase-0 code the default base for every
agent. Either merge PR #1 into `main`, or document/enforce that all work uses
`cursor/phase-0-foundations-9ca9` until merge.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

**Phase:** Meta

- [ ] PR #1 reviewed and merged to `main`, **or** AGENTS.md / handoff notes explicitly require the feature branch
- [ ] New agents cloning the default branch see Phase 0.5 docs (`DATA_CONTRACT`, tickets, etc.)
- [ ] BACKLOG.md “Base branch” line updated after merge
